**Full Outer:** matches rows based on a condition, returning all matches plus non-matches (filled with NULLs).
**Cross Join:** Creates a Cartesian product, pairing every row from the first table with every row from the 
    second, producing a much larger, unconditional result set.